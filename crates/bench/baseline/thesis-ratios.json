{
  "schema": 1,
  "updated": "2026-04-07",
  "_README": [
    "This file is HAND-EDITED ONLY. No script writes to it.",
    "Each ratio enforces a structural property of PowDB that should not silently degrade.",
    "Raising a ceiling means the thesis gave ground — justify it in the commit message.",
    "Convention: 'bench: relax <ratio_name> <old> -> <new> (<why>)'."
  ],
  "ratios": {
    "powql_point_over_btree_lookup": {
      "numerator": "powql_point",
      "denominator": "btree_lookup",
      "ceiling": 2.5,
      "observed_at_last_edit": 2.055,
      "meaning": "Full PowQL parser+planner+executor overhead over the raw B-tree probe. This is THE thesis metric. First captured from GHA run 24065496323 on ubuntu-24.04: 3764ns / 1832ns = 2.055x. Ceiling 2.5 leaves ~22% headroom for incidental overhead but not enough to hide a planner regression that un-folds .field=literal into SeqScan+Filter. Smoke bench on M1 measured this same ratio at 1.897-2.14x — the ratio is hardware-stable."
    }
  }
}
